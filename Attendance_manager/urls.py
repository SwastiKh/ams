from django.urls import path
from . import views
from django.contrib.staticfiles.urls import staticfiles_urlpatterns
from django.conf import settings
app_name="Attendance"

urlpatterns=[
    # path('search/',views.search,name="search"),
    # path('teacher/',views.teacher,name='teacher'),
    # path('student/',views.student,name='student'),
    # path('teacher/add_attendance',views.add_attendance,name='add_attendance'),
    # path('teacher/edit_attendance',views.edit_attendance,name='edit_attendance'),
    # path('teacher/view_attendance',views.view_attendance,name='view_attendance'),
    # path('student/view_attendance',views.view_attendance,name='view_attendance'),

]
